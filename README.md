# Graph

A visualization tool that displays package dependencies of @wxn0brp packages in an interactive graph. It shows how different packages relate to each other with different connection types (dependencies, dev dependencies, peer dependencies, etc.) and allows you to explore the relationships between them.

## Features

- Interactive force-directed graph visualization
- Color-coded dependency types
- Real-time exploration of package relationships
- Zoom and pan functionality
- Tooltips with detailed dependency information

## Setup

1. `bun install`
1. `suglite`

Or use the hosted version: [https://wxn0brp.github.io/graph/](https://wxn0brp.github.io/graph/)